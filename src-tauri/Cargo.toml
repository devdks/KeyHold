[package]
name = "keyhold"
version = "0.1.0"
description = "A compact desktop utility that holds a selected key"
authors = ["devdks"]
edition = "2021"

[lib]
name = "keyhold_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
enigo = "0.6"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-global-shortcut = "2"
